{
    "description": "User-script extensions for the MarkEdit Markdown editor",
    "engines": {
        "node": ">=20"
    },
    "name": "markedit-extensions",
    "private": true,
    "scripts": {
        "test": "node --test",
        "lint": "oxlint",
        "format": "oxfmt",
        "format:check": "oxfmt --check"
    },
    "version": "1.1.0",
    "devDependencies": {
        "oxfmt": "0.63.0",
        "oxlint": "1.78.0"
    }
}
